{
  "name": "codepath-app",
  "version": "1.0.0",
  "description": "CodePath empaquetado como app nativa con Capacitor",
  "scripts": {
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
